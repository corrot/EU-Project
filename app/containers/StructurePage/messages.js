/*
 * StructurePage Messages
 *
 * This contains all the text for the StructurePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StructurePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StructurePage container!',
  },
});
