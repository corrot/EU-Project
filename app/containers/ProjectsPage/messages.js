/*
 * ProjectsPage Messages
 *
 * This contains all the text for the ProjectsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProjectsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProjectsPage container!',
  },
});
