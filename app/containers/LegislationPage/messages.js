/*
 * LegislationPage Messages
 *
 * This contains all the text for the LegislationPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LegislationPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LegislationPage container!',
  },
});
