/*
 * CooperationPage Messages
 *
 * This contains all the text for the CooperationPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CooperationPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CooperationPage container!',
  },
});
